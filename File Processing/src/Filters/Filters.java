package Filters;

/**
 * this enum contains the names of all filters 
 * @author Assaf
 *
 */
public enum Filters {
	greater_than,
	between,
	smaller_than,
	file_name,
	contains,
	prefix,
	suffix,
	writeable,
	executable,
	hidden,
	all;
}
